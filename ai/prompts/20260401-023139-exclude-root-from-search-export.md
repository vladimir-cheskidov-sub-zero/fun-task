lines 47 from @src/Domain/Category/Visitor/SearchIndexExportVisitor.php The visitor never excludes the technical `root` node explicitly. The spec’s global assumptions say `root` is just a tree container, so if someone adds `searchable` to the root later, this code will emit a bogus search document for it. Please guard with `isRoot()` here and add a regression test that keeps the root out of the export even when it is otherwise searchable.
